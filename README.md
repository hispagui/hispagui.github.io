## Deploy
   - Keep it Public.
   - Go to the repo's **Settings → Pages**. Under "Build and deployment", Source should be "Deploy from a branch", branch `main`, folder `/root`.
   - Save.
   - Any time you want to update content, edit the file on GitHub directly, the live site updates automatically
within a minute or two.
